import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com','d22fxaf9t8d39k.cloudfront.net','thecandleshop.com.ar'], // Agrega aquí los dominios permitidos
  },
};

export default nextConfig;
